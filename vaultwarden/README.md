# Vaultwarden

```sh
VAULTWARDEN_VERSION=latest
VAULTWARDEN_SIGNUPS_ALLOWED=true # Controls if new users can register
VAULTWARDEN_DATA_LOCATION=~/vaultwarden
VAULTWARDEN_DOMAIN=https://vaultwarden.yourdomain.ru # The domain must match the address from where you access the server
```
